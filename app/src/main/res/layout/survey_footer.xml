<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:paddingEnd="24dp"
    android:paddingStart="24dp">

    <TextView
        android:id="@+id/text_view_submit"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/shape_floating_button"
        android:backgroundTint="@color/black"
        android:foreground="?attr/selectableItemBackground"
        android:elevation="40dp"
        android:gravity="center"
        android:text="결과 제출"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/text_view_show_result"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/text_view_show_result"
        android:layout_width="100dp"
        android:layout_height="0dp"
        android:layout_marginStart="30dp"
        android:background="@drawable/shape_floating_button"
        android:elevation="40dp"
        android:gravity="center"
        android:text="결과만 간단히 보기"
        android:textColor="@color/text_floating"
        android:textSize="16dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toEndOf="@+id/text_view_submit"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>